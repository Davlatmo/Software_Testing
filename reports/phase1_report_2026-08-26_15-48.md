# Phase 1 — Requirements Compliance Report
## Result: NON-COMPLIANT
### Summary
The commit only changes comment text in techshop_testing/Server.py (two minor wording edits) and does not implement any functional behavior required by the requirements document. No new validation, error responses, or filter logic required by REQ-01..REQ-05 were added in this commit.
### Missing requirements
- REQ-01: Search by name — no code changes implementing case-insensitive search, empty/whitespace handling, or returning empty array when no match
- REQ-02: Filter by category — no enforcement of valid categories (electronics, accessories) and no 400 response for invalid categories
- REQ-03: Filter by price range — no validation for minPrice > maxPrice or 400 response with specified error message
- REQ-04: Combined filters — no evidence of tests/changes ensuring filters correctly combine; commit does not add or change logic for combining filters
- REQ-05: Single product endpoint — commit only changes wording in comment; no changes ensuring 404 + { "error": "Product not found" } when product absent
### Errors found
- Server.py: only comment text changed at get_products docstring — diff hunk @@ -115,7 +115,7 @@ shows a wording change from "REQ-05 (single product) is handled by get_product() below." to "REQ-05 -> single product is handled by get_product() below.". No functional code added (lines near 115).
- Server.py: only comment text changed around category filter — diff hunk @@ -152,7 +152,7 @@ changes comment from "# REQ-02: filter by category (already lowercased above)" to "# REQ-02: filter by category -> already lowercased above". No validation for allowed categories or error handling was added (lines near 152).
- No new code present in the commit to: perform case-insensitive name matching, treat empty/whitespace search as 'all products', validate category against allowed list and return 400 with { "error": "Invalid category" }, validate price ranges and return 400 with { "error": "minPrice cannot be greater than maxPrice" }, or ensure get_product() returns 404 with { "error": "Product not found" }.
### Suggestions
- Implement search behavior in techshop_testing/Server.py:get_products() (around where request.args are read). Ensure search term is stripped and lowercased; if stripped term is empty, skip filtering and return all products. Use case-insensitive matching: if search is provided, filter with: term = search.strip().lower(); product names compared using p['name'].lower().
- Validate category parameter in get_products(): explicitly check category in {'electronics','accessories','all'}. If category is provided and not in allowed set, return (jsonify({"error": "Invalid category"}), 400). Apply category filtering only when valid and not 'all'.
- Add price range handling in get_products(): parse minPrice and maxPrice from request.args to floats/ints with safe error handling; if both provided and minPrice > maxPrice return (jsonify({"error": "minPrice cannot be greater than maxPrice"}), 400). When valid, filter products with inclusive range checks.
- Ensure combined filters work by applying filters sequentially on the same 'filtered' list (search, then category, then price) so they intersect correctly. Add unit tests for combined case like '?search=keyboard&category=electronics&maxPrice=150'.
- In techshop_testing/Server.py:get_product(id) ensure that when product not found the endpoint returns (jsonify({"error": "Product not found"}), 404). Confirm endpoint returns JSON for success and error cases.
- Add input parsing error handling (e.g., non-numeric minPrice/maxPrice) and return 400 with a clear JSON error message.
- Consider adding helper functions (e.g., validate_category(), parse_price_param()) to keep get_products() concise and testable, and add unit tests covering all REQ-* acceptance criteria.
### Quality notes
- The commit touches only comments — no functional change — which is fine for documentation but does not address feature requirements.
- Existing names (PRODUCTS, get_products, get_product) are clear; using helper functions for validation/filtering would improve readability and testability.
- Make error responses consistent JSON objects across endpoints and include explicit status codes as required by the acceptance criteria.
- Add unit/integration tests exercising each requirement (case-insensitive search, category validation, price validation, combined filters, single-product 404) to prevent regressions.
