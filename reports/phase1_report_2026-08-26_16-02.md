# Phase 1 — Requirements Compliance Report
## Result: NON-COMPLIANT
### Summary
The commit only adds import math and guards against NaN/Inf when parsing minPrice/maxPrice, and a minor docstring edit. It does not implement or demonstrate the majority of the requirements (search, category validation, minPrice>maxPrice handling, combined filters verification, single-product endpoint behavior, and acceptance criteria).
### Missing requirements
- REQ-01: Search by name — case-insensitive matching; empty/whitespace search returns all products; return empty array (200) when no match.
- REQ-02: Filter by category — validate categories ('electronics', 'accessories') and return 400 with { "error": "Invalid category" } for invalid values.
- REQ-03 (partial): Price range — check and return 400 with { "error": "minPrice cannot be greater than maxPrice" } when minPrice > maxPrice. (Parsing numeric values was partly addressed, but this required validation is missing.)
- REQ-04: Combined filters — ensure search, category and price filters are all applied together correctly (no evidence in the diff that combined-filter logic is implemented/verified).
- REQ-05: Single product endpoint — GET /api/products/<id> must return a single product or 404 with { "error": "Product not found" } when missing (no changes in diff show this behavior).
- Acceptance criteria: Ensure all endpoints return JSON and that search is case-insensitive (not shown in the commit).
### Errors found
- Server.py (@ diff hunk @@ -2,6 +2,7 @@) — added `import math` (good), but the diff shows no other functional changes implementing the feature set.
- Server.py (@ diff hunk @@ -115,7 +116,7 @@) — only a docstring grammar change: 'single products' instead of meaningful behavior change for REQ-05. (Line referenced in diff: around line 116.)
- Server.py (@ diff hunk @@ -137,12 +138,16 @@) — added NaN/Inf checks when parsing minPrice and maxPrice (good), but there is no follow-up check to enforce minPrice <= maxPrice and return the required 400 error and JSON message. (The parsing logic appears around lines ~138–150 in the diff.)
- No evidence in the diff that search matching is case-insensitive (REQ-01), or that empty/whitespace search returns all products.
- No evidence in the diff of category validation against the allowed list ('electronics', 'accessories') or returning the exact required error JSON on invalid category (REQ-02).
- No evidence the code serializes all responses as JSON (acceptance criterion) or that the single-product endpoint returns the exact required error payload for missing product (REQ-05).
### Suggestions
- Enforce minPrice <= maxPrice after parsing: in Server.py, inside get_products() after parsing both min_price and max_price (diff hunk around @@ -137,12 +138,16 @@), add:

    if min_price is not None and max_price is not None and min_price > max_price:
        return jsonify({"error": "minPrice cannot be greater than maxPrice"}), 400

  This implements the missing part of REQ-03 and must be placed after both min/max parsing blocks.
- Implement case-insensitive search and empty-search handling in get_products(): when reading search (around line shown @@ -115,7 +116,7 @@), normalize the term and product names, e.g.:

    if search is not None:
        term = search.strip().lower()
        if term == "":
            term = None   # treat empty/whitespace as no search filter

    ...when filtering products: use product['name'].lower() and check `term in product['name'].lower()`.

  This will satisfy REQ-01 (case-insensitive and empty search -> return all products).

- Validate category against allowed list and return required error: after reading category in get_products() (near the start of the function), add:

    VALID_CATEGORIES = {"electronics", "accessories"}
    if category is not None:
        if category not in VALID_CATEGORIES:
            return jsonify({"error": "Invalid category"}), 400

  This implements REQ-02 and returns the exact JSON and 400 status required.
- Ensure combined-filters apply in a single pipeline: apply category, search and price filters cumulatively when building the filtered list (REQ-04). Prefer a single pass or chain filters so the example `?search=keyboard&category=electronics&maxPrice=150` returns correct results.
- Implement GET /api/products/<id> behavior (REQ-05): add or verify a route function get_product(product_id) that finds the product by id and returns jsonify(product) with 200, or returns `jsonify({"error": "Product not found"}), 404` when missing. If such a function exists, ensure its error payload and status code match the requirement exactly.
- Ensure all endpoints return JSON: wrap all responses in Flask jsonify(), including empty arrays for no matches (e.g., return jsonify([]), 200). Add unit tests or integration tests that assert Content-Type is application/json and bodies match expected JSON shapes.
- Replace magic strings with constants (VALID_CATEGORIES, error messages) at module top so messages are consistent and easy to test.
- Add unit tests covering: case-insensitive search, whitespace search, invalid category, minPrice>maxPrice, combined filters, single-product 404. This will prevent regressions and verify acceptance criteria.
### Quality notes
- Positive: The added NaN/Inf checks for minPrice/maxPrice (Server.py diff hunk @@ -137,12 +138,16 @@) are a good defensive improvement to avoid float('nan') or infinities being accepted.
- Minor: The docstring edit at @@ -115,7 +116,7 @@ is purely grammatical and doesn't affect behavior.
- Missing: The commit is very small and appears to be an incremental change; however, it does not meaningfully implement the required feature set. Larger, focused commits that implement one requirement at a time (with tests) would be easier to review.
- Maintainability: Use module-level constants for categories and error messages, and factor filter logic into helper functions to simplify get_products() and make unit testing easier.
- Testing: There are no test additions in this diff; tests are necessary to assert the exact error payloads and combined-filter behaviors required by the spec.
