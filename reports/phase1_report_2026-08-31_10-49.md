# Phase 1 — Requirements Compliance Report
## Result: NON-COMPLIANT
### Summary
The commit partially implements the product filters (category and numeric parsing/error responses), but it introduces regressions and deviates from a few explicit requirements: NaN/Inf numeric validation was removed, the code now accepts a special "all" category that is not allowed by REQ-02, and there is no evidence in the diff that case-insensitive name search and combined filters are correctly implemented. Fixes are required before the feature fully meets the requirements.
### Missing requirements
- REQ-01: No confirmed implementation of case-insensitive search-by-name (GET /api/products?search=<term>) in the changed code (no search-filtering shown in the diff).
- REQ-04: No evidence that search, category and price filters are applied together correctly (combined filtering not shown in the diff).
- REQ-02: Strict category validation disallows only 'electronics' and 'accessories' per requirements — the committed code explicitly allows 'all' which is not listed as a valid category in the requirements.
- Numeric validation: Rejection of NaN/Infinity for minPrice and maxPrice is missing (the previous math.isnan/isinf checks were removed).
- REQ-05: There is a reference to get_product() handling single-product endpoint, but no diff evidence that GET /api/products/<id> returns 404 with { "error": "Product not found" } when missing.
### Errors found
- techshop_testing/Server.py: import 'math' was removed (diff hunk @@ -2,7 +2,7 @@). The commit also removed checks for math.isnan/math.isinf when parsing minPrice and maxPrice (diff hunk @@ -135,29 +135,25 @@). As a result, inputs like 'nan' or 'inf' will be accepted by float(...) and not rejected — this violates expected numeric input validation and can cause incorrect filter behavior.
- techshop_testing/Server.py: get_products() now treats category == 'all' as a special allowed value and bypasses validation (diff hunk @@ -114,20 +114,20 @@). REQ-02 explicitly lists valid categories as only 'electronics' and 'accessories'; accepting 'all' changes the specified behavior and should not be allowed unless requirements are updated.
- techshop_testing/Server.py: The diff shows category filtering using equality (filtered = [p for p in filtered if p["category"] == category]) but does not show any normalization of product category values. If product.category values are not already lowercase, this equality may fail. The requirements require category filtering to return products in specified category — the code relies on implicit data normalization which is fragile (diff hunk @@ -135,29 +135,25 @@).
- Missing evidence in diff that REQ-01 (case-insensitive search) and REQ-04 (combined filters) are implemented. The provided diff shows only category filtering being applied; search and price filtering are not visible in this commit, so it is unclear whether search is case-insensitive and whether filters are combined correctly.
### Suggestions
- Reintroduce robust numeric validation for minPrice and maxPrice. In techshop_testing/Server.py inside get_products() (around the block shown in diff @@ -135,29 +135,25 @@):
- import math at top of the file (restore the removed import) and after converting to float, explicitly reject NaN and infinite values, for example:
    try:
        min_price = float(min_price_raw)
        if math.isnan(min_price) or math.isinf(min_price):
            raise ValueError
    except ValueError:
        return jsonify({"error": "minPrice must be a number"}), 400
  Do the same for maxPrice.
Or use math.isfinite(min_price) to accept only finite numbers.
- Remove the special-case acceptance of 'all' for the category parameter to match REQ-02. In get_products() (diff @@ -114,20 +114,20 @@) change the validation from:
    if category:
        category = category.lower()
        if category != "all" and category not in VALID_CATEGORIES:
            return jsonify({"error": "Invalid category"}), 400
  to simply:
    if category:
        category = category.lower()
        if category not in VALID_CATEGORIES:
            return jsonify({"error": "Invalid category"}), 400
  If you intended to implement an 'all' shortcut, either update the requirements or handle it entirely on the client side; do not accept inputs not listed in requirements.
- Ensure search is implemented and is case-insensitive. Add code in get_products() after category filtering (or before, order doesn't matter but must be combined) to apply search when provided, e.g.:
    if search and search.strip():
        s = search.strip().lower()
        filtered = [p for p in filtered if s in p.get('name', '').lower()]
  Return all products when search is empty or only whitespace (per REQ-01).
- Implement price-range filtering (inclusive) and ensure combination with other filters. After parsing and validating min_price and max_price, apply:
    if min_price is not None:
        filtered = [p for p in filtered if p['price'] >= min_price]
    if max_price is not None:
        filtered = [p for p in filtered if p['price'] <= max_price]
  Ensure p['price'] exists and is numeric in the PRODUCTS data.
- Verify and add tests for combined filter scenarios as per REQ-04 (e.g. '?search=keyboard&category=electronics&maxPrice=150') to ensure filters are applied conjunctively.
- Confirm the single-product endpoint (get_product()) returns 404 with { "error": "Product not found" } for missing IDs. If not present, implement in Server.py a lookup that returns (jsonify({"error": "Product not found"}), 404) when an ID is absent.
### Quality notes
- The code returns clear JSON error messages for invalid numeric parsing and invalid categories (where validation is present).
- Whitespace and minor formatting inconsistent in the diff (several lines have changed indentation/spacing). These are cosmetic but reduce readability; run an auto-formatter (e.g. black) to normalize style.
- Variable names are descriptive (category, search, min_price, max_price) and error messages are user-friendly.
- There is a helpful docstring on get_products() referencing requirements; good practice to keep requirements traceable in code.
- Consider centralizing validation logic (e.g. helper functions) for reuse and easier testing (validate_category(), parse_price_param()).
