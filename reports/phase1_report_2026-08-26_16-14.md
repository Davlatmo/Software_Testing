# Phase 1 — Requirements Compliance Report
## Result: NON-COMPLIANT
### Summary
The commit only modifies number parsing for minPrice/maxPrice by adding math checks, but it introduces indentation/syntax problems that will cause the server to crash. The change does not demonstrate that the overall feature requirements (search, category filtering, combined filters, single product endpoint) are implemented or validated in this commit — at best it only augments numeric parsing. Because of the syntax/indentation issues the API will fail at runtime and therefore cannot satisfy the requirements until fixed.
### Missing requirements
- The commit does not add or show any tests or code changes that validate REQ-01 (search by name) — cannot confirm case-insensitive matching or handling of empty/whitespace search.
- The commit does not add or show handling/validation for REQ-02 (invalid category returns 400 with { "error": "Invalid category" }).
- The commit does not add or show any tests or verification for REQ-04 (combined filters working together).
- The commit does not touch or show REQ-05 (single product endpoint) or add tests for 404 behavior.
- No acceptance tests or explicit checks ensuring all endpoints return JSON were added in this commit.
### Errors found
- Server.py: the edits introduce broken indentation inside get_products() around the minPrice parsing block (diff hunk @@ -134,18 +135,22 @@). The 'try:' line remains but the following 'min_price = float(min_price_raw)' line is mis-indented relative to the try, and the 'except ValueError:' and its return are also mis-indented. This will raise an IndentationError / SyntaxError at runtime.
- Server.py: same indentation problem in the maxPrice parsing block (diff hunk @@ -134,18 +135,22 @@). The 'max_price = float(max_price_raw)' line and the following math checks/except are mis-indented relative to the try/except structure.
- Server.py: a leftover inline comment with an arrow appears in code: '# ← add this line' (diff hunk @@ -134,18 +135,22 @@). This is a cosmetic issue but suggests the change was not cleaned up. While comments are allowed, this one is unprofessional and may indicate copied debug annotations.
- Server.py: the added checks use math.isnan() and math.isinf() to detect invalid float values. Using math.isfinite(value) would be clearer and simpler (isfinite returns False for NaN and Inf).
- Server.py: whitespace-only reformatting (category = request.args.get("category") and search = ...) is fine but makes the diff noisy; it would be better to keep changes focused.
- Because of the syntax/indentation issues the API will not start, so none of the requirements (search, category filter, price filter, combined filters, single product endpoint) can be validated against a running app.
### Suggestions
- Fix indentation in Server.py inside get_products() around the minPrice parsing block. Ensure the try/except block is correctly formed. For example:
  if min_price_raw is not None:
      try:
          min_price = float(min_price_raw)
          if not math.isfinite(min_price):
              raise ValueError
      except ValueError:
          return jsonify({"error": "minPrice must be a number"}), 400
- Apply the same corrected pattern for maxPrice parsing. Example:
  if max_price_raw is not None:
      try:
          max_price = float(max_price_raw)
          if not math.isfinite(max_price):
              raise ValueError
      except ValueError:
          return jsonify({"error": "maxPrice must be a number"}), 400
- Replace the pair of math.isnan(...) or math.isinf(...) checks with math.isfinite(value) to make intent clearer and code shorter.
- Remove the debug/arrow comment '# ← add this line' from the committed code.
- Run the server and add unit/integration tests for the following cases (at minimum):
  - search is case-insensitive and empty/whitespace returns all products (REQ-01),
  - valid and invalid categories, with invalid returning 400 and {"error":"Invalid category"} (REQ-02),
  - numeric edge cases: valid numbers, strings, NaN, Inf for minPrice/maxPrice, and correct 400 responses (REQ-03),
  - combined filters (REQ-04), and
  - single product 200 and 404 behavior (REQ-05).
- Run a linter (flake8/pyflakes) and unit tests before committing to catch syntax/indentation errors early.
- If the rest of get_products() already implements the search/category/combined logic, include tests demonstrating that functionality in the same commit or in a linked commit so reviewers can verify compliance.
### Quality notes
- The intent of adding checks against NaN/Infinity for minPrice/maxPrice is good — it tightens input validation.
- However, the commit introduces indentation inconsistencies that make the change invalid Python; this indicates the changes were not tested locally before committing.
- The commit contains small cosmetic noise (whitespace-only changes and an inline arrow comment) which reduces clarity in the diff.
- No tests or examples were included to demonstrate the new numeric checks or the full set of feature requirements — adding tests would significantly improve confidence and reviewability.
