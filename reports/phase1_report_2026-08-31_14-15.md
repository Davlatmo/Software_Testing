# Phase 1 — Requirements Compliance Report
## Result: NON-COMPLIANT
### Summary
The commit only changes a single error message string and a comment in techshop_testing/Server.py; it does not implement the product search & filtering feature set from the requirements. Worse, it breaks REQ-02 by changing the exact error text the API must return.
### Missing requirements
- REQ-01: Implementation of search-by-name (GET /api/products?search=<term>) — no changes in this commit implement or verify case-insensitive name matching or handling of empty/whitespace search terms.
- REQ-02: Exact error response for invalid category — requirements demand { "error": "Invalid category" } but the commit returns "Invalid category!".
- REQ-02: Assurance that VALID_CATEGORIES contains exactly the allowed values (electronics, accessories) is not shown/verified in this commit.
- REQ-03: Parsing and validation of minPrice and maxPrice (including returning 400 when minPrice > maxPrice) is not implemented in this commit (only a comment about parsing was edited).
- REQ-04: Combined filters (search + category + price) are not implemented / not modified in this commit.
- REQ-05: Single product endpoint (GET /api/products/<id>) and the required 404 body are not implemented / not modified in this commit.
- Acceptance criteria: Endpoints returning JSON, case-insensitive search, and combinable filters are not implemented or validated in this commit.
### Errors found
- techshop_testing/Server.py (hunk around line 129): The response for an invalid category was changed from return jsonify({"error": "Invalid category"}), 400 to return jsonify({"error": "Invalid category!"}), 400. This violates REQ-02 which requires the exact JSON { "error": "Invalid category" }.
- techshop_testing/Server.py (hunk around line 129): The code special-cases category == "all" (if category != "all" and category not in VALID_CATEGORIES). REQ-02 defines valid categories only as 'electronics' and 'accessories' — allowing 'all' is a behavioral mismatch unless requirements are updated. The commit does not document this change.
- techshop_testing/Server.py (immediately after hunk): A comment was edited (# REQ-03: parse price parameters explicitly ...) but there is no accompanying code change implementing the price parsing/validation described by REQ-03. Leaving only a comment suggests incomplete implementation.
- General: The commit contains no changes that implement or verify REQ-01, REQ-03, REQ-04, REQ-05 or the acceptance criteria; the single functional change is an API-contract-breaking string change.
### Suggestions
- Revert the invalid-category error text to match the requirements exactly: in techshop_testing/Server.py (hunk around line 129) change return jsonify({"error": "Invalid category!"}), 400 back to return jsonify({"error": "Invalid category"}), 400.
- If 'all' is intended as a valid query value, update the requirements document to list 'all' as an accepted category OR remove the special-case handling for 'all' in techshop_testing/Server.py so only the allowed categories ('electronics', 'accessories') are accepted. Concretely, replace the condition if category != "all" and category not in VALID_CATEGORIES: ... with if category not in VALID_CATEGORIES: ... (or adjust VALID_CATEGORIES to include 'all' and document it).
- Implement price parsing/validation in get_products: convert minPrice and maxPrice to numbers with explicit try/except and return 400 with JSON error messages for invalid numeric input. Also enforce REQ-03's minPrice > maxPrice check and return return jsonify({"error": "minPrice cannot be greater than maxPrice"}), 400.
- Implement search-by-name (REQ-01) with case-insensitive matching. Example approach: normalize both product.name and the search term with .lower(), treat empty or whitespace-only search terms as returning all products.
- Ensure combined filters work together (REQ-04): apply search, category, and price filters in sequence (or via a single filter predicate) so the final result satisfies all provided constraints.
- Implement the single-product endpoint GET /api/products/<id> (REQ-05) to return the product JSON or return jsonify({"error": "Product not found"}), 404 when not present.
- Add unit/integration tests covering each REQ (search behavior, category validation, price range parsing and validation, combined filters, single product retrieval) and tests verifying exact JSON error bodies and status codes.
- Add a short comment in the code noting that error strings are part of the public API contract so they must match requirements precisely (to avoid accidental punctuation changes).
### Quality notes
- The change is small and localized — variable naming and the comment edit improve clarity ('parameters' instead of 'params').
- However, the commit changed an API contract (error message text) in a non-backwards-compatible way; public API error messages should be treated as part of the contract and not altered without updating requirements/versioning documentation.
- There are signs of planned work for REQ-03 (comment added), but the implementation is incomplete in this commit. Commits that only change comments without implementing the corresponding logic can be misleading.
- No tests or additional files were added in this commit to show validation of behaviors; adding tests for exact error payloads and edge cases would greatly improve reliability.
