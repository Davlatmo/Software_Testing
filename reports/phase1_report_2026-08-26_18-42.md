# Phase 1 — Requirements Compliance Report
## Result: NON-COMPLIANT
### Summary
The commit changes techshop_testing/Server.py (get_products) in ways that break or weaken requirements validation. It allows a special "all" category (contradicting REQ-02), and it removed numeric-sanity checks for minPrice/maxPrice (so NaN/Infinity values can pass), while also removing the math import. Other requirements (search case-insensitivity, combined filters, single product endpoint) are not affected by these hunks as shown, but the introduced regressions make the feature non-compliant with the requirements for input validation and category handling.
### Missing requirements
- REQ-02: Strict category validation — the code now accepts category="all" instead of restricting to only 'electronics' or 'accessories' and returning 400 for invalid categories.
- REQ-03 (robust numeric validation): Rejection of non-finite numbers (NaN, Infinity) for minPrice/maxPrice. The commit removed checks that would reject NaN/Infinity, allowing these invalid numeric inputs to pass instead of returning 4xx.
### Errors found
- techshop_testing/Server.py @@ -2,7 +2,7 @@ (top of file): removed 'import math' but later code previously relied on math.* checks. Removing the import introduces a regression if math.* checks are used or intended.
- techshop_testing/Server.py @@ -114,20 +114,20 @@ (function get_products): category validation was changed to allow the special value 'all'. The new line if category != "all" and category not in VALID_CATEGORIES: (see diff) means 'all' is treated as valid input. The requirements explicitly list valid categories as only 'electronics' and 'accessories' and require returning 400 for invalid categories.
- techshop_testing/Server.py @@ -135,29 +135,25 @@ (function get_products): the commit removed the numeric-sanity checks for minPrice and maxPrice. Previously the code attempted to check math.isnan(...) or math.isinf(...) and reject those values; the new code only uses float(...) and returns 400 only on ValueError. This allows strings like 'NaN' or 'Infinity' (which float() accepts) to become float('nan')/float('inf') and not be rejected, violating the expectation that invalid numeric inputs should return a 4xx status.
### Suggestions
- techshop_testing/Server.py: Re-add robust numeric validation for minPrice/maxPrice. E.g. re-import math at the top (import math) and after conversion do: if not math.isfinite(min_price): return jsonify({"error": "minPrice must be a finite number"}), 400 (and same for max_price). Alternatively use math.isfinite() which covers NaN and Infinity in one call.
- techshop_testing/Server.py: Enforce the requirement that only 'electronics' and 'accessories' are valid categories. Replace the current check with: if category and category.lower() not in VALID_CATEGORIES: return jsonify({"error": "Invalid category"}), 400. Remove the special-case allowance for 'all' unless you intend to change the requirements (in which case update the requirements file and tests).
- techshop_testing/Server.py: Make error messages and validation explicit. For numeric parsing, keep the current ValueError handling for non-numeric strings, and add finite-number checks. Example flow: try -> val=float(raw); except ValueError -> 400; if not math.isfinite(val) -> 400.
- If 'all' is intended as a feature, update the requirements.md to include 'all' as a valid category option and add acceptance criteria describing expected behavior. Otherwise remove support for 'all' from the code.
- Verify and/or add tests that cover: category invalidation (e.g., category=toys -> 400), category cases (Electronics vs electronics), minPrice/maxPrice with 'NaN'/'Infinity' and with minPrice>maxPrice, and combined filters (search + category + price) to ensure combined filter behavior continues to work.
- Confirm search implementation is case-insensitive (p['name'].lower() comparison) and add/keep unit tests. If not present, implement search normalization: search_term = search.strip().lower() and compare with product name lowercased.
### Quality notes
- There are lingering comments/artifacts in the diff (e.g. '← add this line') from previous editing. Remove such comments to improve clarity.
- Whitespace and indentation changes were made in this commit; they are harmless but noisy. Keep style consistent to avoid distracting diffs.
- Validation logic would be clearer if extracted into small helper functions (e.g., parse_finite_float(raw) -> (value or raises/returns error)), improving testability and readability.
- The code currently special-cases 'all' for category filtering. If 'all' should be supported, document it in requirements; otherwise, the special-case should be removed to match requirements.
