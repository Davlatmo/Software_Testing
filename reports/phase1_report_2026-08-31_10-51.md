# Phase 1 — Requirements Compliance Report
## Result: NON-COMPLIANT
### Summary
The commit contains only small refactors to techshop_testing/Server.py (mainly docstring/whitespace and changes to numeric parsing). It does not clearly demonstrate full, tested implementation of the feature requirements. In particular, I could not verify correct search handling (case-insensitive matching and empty/whitespace behavior), combined-filter behavior, and the single-product endpoint behavior from the provided diff. There is also a functional/semantic change to category handling (special-casing "all") that deviates from the stated requirements and should be reviewed.
### Missing requirements
- REQ-01: Explicit implementation details showing search-by-name is case-insensitive and that empty/whitespace search returns all products (not verifiable in this diff).
- REQ-04: Evidence that search, category and price filters are correctly combined (no code in the diff shows combined-filter logic being applied or tested).
- REQ-05: Evidence that GET /api/products/<id> returns JSON and returns 404 with { "error": "Product not found" } when missing (single-product handler not shown/changed in this diff).
### Errors found
- techshop_testing/Server.py: Top import hunk (diff header @@ -2,7 +2,7 @@) — the commit removed 'import math' while earlier code contained NaN/Inf checks (these checks were removed in the same commit). Removing 'math' is fine only if no other code in the file uses math; please confirm the rest of the file no longer references math. If other places still expect math, this will raise a NameError.
- techshop_testing/Server.py: get_products() (hunk @@ -114,20 +114,20 @@ and @@ -135,29 +135,25 @@) — the handler now accepts category == "all" as a special-case (if category != "all" and category not in VALID_CATEGORIES: ...). The requirements state valid categories are 'electronics' and 'accessories' and that invalid categories must return 400 with { "error": "Invalid category" }. Accepting 'all' as a valid category is a deviation from REQ-02 and may allow inputs that should be rejected per requirements.
- techshop_testing/Server.py: get_products() price parsing (hunk @@ -135,29 +135,25 @@) — the commit removed prior explicit checks for NaN/Infinity (previously used math.isnan/math.isinf). If you intentionally removed those checks that's fine, but if you want to reject 'NaN'/'Infinity' textual inputs you must reintroduce those guards or otherwise normalize/validate those inputs. The diff also replaces some indentation; ensure there are no indentation errors introduced (the try/except blocks look correct in the diff, but inconsistent spacing was introduced).
- General: The diff does not show any explicit implementation or tests verifying that search is case-insensitive (REQ-01 acceptance criteria) or that empty/whitespace search returns all products. Without those checks present in the diff, I cannot confirm REQ-01 is satisfied.
### Suggestions
- For REQ-01 (search): In techshop_testing/Server.py inside get_products(), ensure you normalize and strip the search term and perform a case-insensitive substring match against product names. Example approach: search_raw = request.args.get('search'); if search_raw is not None: search = search_raw.strip(); if search == '': treat as no search filter; else compare via product['name'].lower() and search.lower() (or use casefold() for better Unicode behavior). Add unit tests for: exact match, differing case, punctuation, whitespace-only search returning all products, and no-match returning [] with 200.
- For REQ-02 (category): If requirements forbid any category other than 'electronics' and 'accessories', remove the 'all' special-case. Replace the validation with: if category is not None: category_lc = category.lower(); if category_lc not in VALID_CATEGORIES: return jsonify({'error':'Invalid category'}), 400. If you intend to support 'all', update the requirements and tests accordingly.
- For REQ-03 (price parsing): If you want to disallow NaN/Infinity, reintroduce explicit checks after float conversion, e.g. use import math and then if math.isnan(min_price) or math.isinf(min_price): raise ValueError, and similarly for max_price. Otherwise document that 'NaN'/'Infinity' inputs will be treated as parsing errors or accepted behaviour.
- For REQ-04 (combined filters): Ensure filters are applied in sequence on the same filtered list (category → price → search, or any order) and that combined queries produce the intersection of constraints. Add tests for combined queries like '?search=keyboard&category=electronics&maxPrice=150' verifying results.
- For REQ-05 (single product): Verify the single-product endpoint (likely get_product()) returns JSON and returns 404 with body { "error": "Product not found" } when the id is not present. Add/confirm tests for an existing id and for a missing id.
- General code hygiene: Remove stray inline comments like '← add this line' from the code. Make whitespace and indentation consistent. Add unit tests for all 4xx error conditions and acceptance criteria stated in the requirements file.
### Quality notes
- Positive: The code uses a VALID_CATEGORIES set and early validation which is a clear approach and aids readability.
- Positive: Error responses use jsonify(...) and return status codes which aligns with the requirement that endpoints return JSON.
- Improvement: There are leftover comment artifacts (e.g., '← add this line') in the original diff context; these should be cleaned up to avoid confusion in future diffs.
- Improvement: Consider using search.casefold() and product['name'].casefold() for robust Unicode case-insensitive matching.
- Improvement: Add unit tests or integration tests covering the acceptance criteria (case-insensitive search, combination of filters, invalid inputs, single-product 404) and include them in the commit.
