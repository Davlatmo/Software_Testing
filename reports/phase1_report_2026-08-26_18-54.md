# Phase 1 — Requirements Compliance Report
## Result: COMPLIANT
### Summary
The committed changes in techshop_testing/Server.py implement the product search and filtering feature and satisfy REQ-01 through REQ-05 and the acceptance criteria. The GET /api/products endpoint supports case-insensitive search (with whitespace-only treated as no filter), category filtering with validation, numeric minPrice/maxPrice parsing and validation, combined filtering, and the single-product endpoint returns 404 when not found.
### Suggestions
- Handle special float values (NaN, +Inf, -Inf) explicitly when parsing minPrice/maxPrice to avoid surprising behavior. The removed import of math suggests this was considered in an earlier edit (see git diff hunk @@ -135,29 +135,25 @@). Example: after float(...) guard with math.isnan/math.isinf and return 400.
- Consider normalizing category check to treat an empty string parameter consistently. Currently request.args.get('category') returns '' if the client sends category=, which is truthy and will be lowercased to '' then fall into the invalid-category branch. If you want to treat empty string as 'no filter', change the initial check from if category: to if category is not None and category.strip() != ''. (See get_products() category handling around diff hunk @@ -114,20 +114,20 @@.)
- Add unit/integration tests that exercise combined filters and edge cases (whitespace search, empty query param values, non-numeric price strings, NaN/Inf). The main logic is in get_products() and get_product() in techshop_testing/Server.py.
- For consistency, consider centralizing API error responses into a small helper (e.g. bad_request(msg)) to keep status codes and JSON format consistent across endpoints. Several places return jsonify({...}), <status> directly (e.g. category invalid, min/max parse errors, product not found).
### Quality notes
- File: techshop_testing/Server.py — get_products() implements the required behavior. Key logic locations:
- - Category validation and lowering: in get_products() around the diff hunk starting @@ -114,20 +114,20 @@ (category = request.args.get("category") ... category = category.lower()).
- - minPrice/maxPrice parsing and numeric validation: in get_products() around the diff hunk starting @@ -135,29 +135,25 @@ (float conversion and returning 400 on ValueError).
- - Search handling: trimming whitespace and case-insensitive substring match is implemented in get_products() with search.strip() and comparing against p["name"].lower().
- - Combined filters applied in sequence (category -> search -> minPrice -> maxPrice) which composes the filters correctly before returning jsonify(filtered).
- The code consistently returns JSON for API routes using Flask's jsonify. Error messages match the requirements (e.g. {"error": "Invalid category"}, {"error": "minPrice cannot be greater than maxPrice"}, and {"error": "Product not found"}).
- Style/maintainability: code is readable and modular within this single-file server. Two @app.after_request decorators exist (set_session_cookie and add_cors_headers) — Flask allows multiple after_request handlers, but if you rely on ordering, consider combining or documenting the order.
- Minor nit: the git diff shows removal of the math import (@@ -2,7 +2,7 @@) — if you plan to validate NaN/Inf later, you will need to re-import math.
