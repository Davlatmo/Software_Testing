# Phase 1 — Requirements Compliance Report
## Result: COMPLIANT
### Summary
The committed changes in techshop_testing/Server.py implement the product search and filtering requirements (REQ-01 through REQ-05). The GET /api/products endpoint supports case-insensitive search with whitespace handling, category filtering with validation for the allowed categories, numeric minPrice/maxPrice parsing with min>max validation, and filters are applied in combination. The single-product endpoint GET /api/products/<id> returns 404 with the required error body when not found.
### Suggestions
- Input validation for NaN/Infinity: The developer removed the math import (see diff header @@ -2,7 +2,7 @@) and no longer checks for NaN/Infinity when parsing minPrice/maxPrice. If you want to explicitly reject 'NaN' or 'Infinity' inputs as non-numeric, re-add the checks in get_products() around the float parsing (hunk shown at @@ -135,29 +135,25 @@). Example concrete fix: in techshop_testing/Server.py, add `import math` near the other imports, then after `min_price = float(min_price_raw)` do `if math.isnan(min_price) or math.isinf(min_price): raise ValueError` (and similarly for max_price) so those values return 400.
- Cart total numeric type: get_cart() currently returns the total as a formatted string (total = f"{total:.2f}"). Consider returning a numeric value (e.g. round(total, 2)) to make it easier for clients to consume as a number instead of parsing a string. File: techshop_testing/Server.py, function get_cart().
- Consider explicitly documenting or removing the special-case category value 'all': The implementation treats category='all' as no filter (see get_products() category validation at @@ -114,20 +114,20 @@ and subsequent filtering). Either document this behavior in requirements or disallow 'all' to avoid ambiguity.
### Quality notes
- Search behavior correctly handles case-insensitive substring matching and ignores whitespace-only searches (techshop_testing/Server.py, get_products() - search handling block).
- Category validation and filtering are implemented and normalized to lowercase; invalid categories return a 400 with the exact error body required (see get_products() category validation in the diff hunk at @@ -114,20 +114,20 @@).
- Price-range parsing enforces numeric input and returns 400 on parse failure; the inclusive comparisons for minPrice/maxPrice are implemented correctly (see parsing and comparisons in get_products() around @@ -135,29 +135,25 @@).
- The single-product endpoint get_product(product_id) returns JSON and the required 404 error body when the product is not found.
- Code is generally clear and readable; session handling is centralized in ensure_session() and the cookie is attached in attach_session_cookie(), which is good structure. There are two @app.after_request functions (set_session_cookie and add_cors_headers) — Flask supports multiple after_request handlers, but be mindful of their execution order if you ever rely on it.
