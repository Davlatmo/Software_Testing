# Phase 1 — Requirements Compliance Report
## Result: NON-COMPLIANT
### Summary
The commit only adds math import and NaN/Inf checks when parsing minPrice/maxPrice and corrects a docstring typo. It does not implement or demonstrate most of the required behaviors from the requirements document (search behavior, category validation, minPrice>maxPrice check, combined filtering logic, single-product endpoint behavior).
### Missing requirements
- REQ-01: Case-insensitive name search (no evidence of search.lower() or trimming/whitespace handling).
- REQ-01: Return all products when search term is empty or only whitespace (no evidence of trimming/empty-term handling).
- REQ-01: Return empty array with status 200 when no products match (no evidence).
- REQ-02: Category validation against allowed values 'electronics' and 'accessories' and returning status 400 with { "error": "Invalid category" } for invalid categories.
- REQ-03: Validation and handling when minPrice > maxPrice returning status 400 with { "error": "minPrice cannot be greater than maxPrice" }.
- REQ-03: Inclusive price-range filtering (no evidence the price comparison uses <= and >=).
- REQ-04: Demonstrable combined filtering of search, category, and price together (no evidence that filters are combined correctly).
- REQ-05: Single product endpoint GET /api/products/<id> returning product JSON or 404 with { "error": "Product not found" } (no changes to or evidence of get_product() implementation in the diff).
- Acceptance: explicit guarantee all endpoints return JSON for the required error messages and responses (no full evidence).
### Errors found
- {'file': 'techshop_testing/Server.py', 'lines': '@@ -2,6 +2,7 @@', 'problem': "Added 'import math' (line with '+import math') — this is fine, but alone does not implement the search or category logic required by the spec."}
- {'file': 'techshop_testing/Server.py', 'lines': '@@ -115,7 +116,7 @@', 'problem': "Docstring change only: 'REQ-05 -> single products is handled by get_product() below.' is a grammatical fix but not a functional change. No evidence get_product() complies with REQ-05 in this diff."}
- {'file': 'techshop_testing/Server.py', 'lines': '@@ -137,12 +138,16 @@', 'problem': "The diff adds NaN/Inf checks for min_price and max_price parsing and returns 400 with messages 'minPrice must be a number' / 'maxPrice must be a number'. However: (a) there is no check for minPrice > maxPrice (required by REQ-03), (b) there is no shown price-filtering logic that applies inclusive comparisons to product prices, and (c) there is no handling for combining price filtering with search and category filters."}
- {'file': 'techshop_testing/Server.py', 'lines': 'general (no explicit line)', 'problem': 'No evidence in the diff of category validation (allowed values) or corresponding 400 error response { "error": "Invalid category" } as required by REQ-02.'}
- {'file': 'techshop_testing/Server.py', 'lines': 'general (no explicit line)', 'problem': 'No evidence of case-insensitive search implementation or trimming of search term/handling whitespace-only terms to return all products (REQ-01).'}
### Suggestions
- In techshop_testing/Server.py inside get_products(), normalize and trim the search term immediately after reading it: search_raw = request.args.get('search'); search = (search_raw or '').strip(); if search == '': search = None. When filtering, use product['name'].lower() and check if search.lower() in product['name'].lower() to ensure case-insensitive matching.
- Add explicit category validation near the start of get_products(): allowed = {'electronics', 'accessories'}; if category is not None and category not in allowed: return jsonify({"error":"Invalid category"}), 400. This fulfills REQ-02 and ensures a 400 response for invalid categories.
- After parsing min_price and max_price (the blocks shown in the diff), add the required check: if min_price is not None and max_price is not None and min_price > max_price: return jsonify({"error":"minPrice cannot be greater than maxPrice"}), 400. Place this check before filtering the product list.
- Ensure price filtering uses inclusive comparisons when filtering products: if min_price is not None and product['price'] < min_price: skip product; if max_price is not None and product['price'] > max_price: skip product. This makes the range inclusive as required by REQ-03.
- Combine filters by applying all selected predicates when building the result list (e.g., for each product apply category check, search check, and price checks in sequence). Ensure that when no filters are provided all products are returned.
- Implement or verify the single-product endpoint get_product(product_id): find the product by id, and return jsonify(product) with status 200 if found; otherwise return jsonify({"error":"Product not found"}), 404. Ensure route is GET /api/products/<id> and response Content-Type is application/json.
- Make sure all error responses use jsonify(...) so the Content-Type is application/json. Keep error message text exactly as specified in requirements for invalid category and minPrice>maxPrice.
- Replace the pattern of raising ValueError inside the try() when detecting NaN/Inf with an explicit check and a direct return for clarity. Example: try: v = float(x) except (ValueError, TypeError): return jsonify({"error":"minPrice must be a number"}), 400; if math.isnan(v) or math.isinf(v): return jsonify({"error":"minPrice must be a number"}), 400.
- Add unit tests that cover: case-insensitive search, whitespace search returning all, invalid category returning 400, minPrice>maxPrice returning 400, inclusive price bounds, combined filters, and single-product 404. This will prevent regressions.
### Quality notes
- Positive: The added NaN/Inf checks (math.isnan, math.isinf) improve robustness for numeric parsing.
- Code clarity: raising ValueError inside the try block (then relying on the except to return) is slightly less clear than doing an explicit check and return; consider separating parsing and validation for readability.
- Docstrings: a minor grammatical change was made but docstrings would be more helpful if they documented expected error messages and HTTP status codes, especially for the required 4xx behavior.
- Testing: there is no evidence in this diff of added tests. Given the number of behavioral requirements, add unit/integration tests to validate the API contract.
- Consistency: ensure all error messages exactly match the requirements where they are specified (e.g., 'Invalid category', 'minPrice cannot be greater than maxPrice', 'Product not found') so callers can rely on string content if tests expect exact matches.
