# Phase 1 — Requirements Compliance Report
## Result: NON-COMPLIANT
### Summary
The commit only adds math import and attempts to reject NaN/Infinity for minPrice/maxPrice in get_products, but it does not implement the bulk of the feature requirements. Additionally, the newly added NaN/Infinity checks contain indentation errors that will cause a SyntaxError at runtime.
### Missing requirements
- REQ-01: Ensure search is case-insensitive and handle empty/whitespace search returning all products / return empty array with 200 when no matches (no change in diff shows implementation or tests).
- REQ-02: Validate category against allowed list (electronics, accessories) and return 400 with { "error": "Invalid category" } for invalid categories (no change in diff shows enforcement).
- REQ-03: Check and return 400 when minPrice > maxPrice with { "error": "minPrice cannot be greater than maxPrice" } (no change in diff implements this logical check).
- REQ-04: Ensure combined filters (search + category + price) work together (no evidence in diff of integration tests or logic change).
- REQ-05: Single product endpoint GET /api/products/<id> must return 404 with { "error": "Product not found" } when missing (diff does not touch this; no evidence it exists/was verified).
- Acceptance: Ensure all endpoints return JSON (no evidence in diff that endpoints guarantee JSON output for all error cases apart from the existing messages).
### Errors found
- {'file': 'techshop_testing/Server.py', 'hunk': '@@ -2,7 +2,7 @@', 'lines': ['import uuid', '+import math', 'import json'], 'problem': "Adding 'import math' is fine, but later changes introduce indentation problems (see below)."}
- {'file': 'techshop_testing/Server.py', 'hunk': '@@ -138,12 +138,16 @@', 'lines': ['        try:', '            min_price = float(min_price_raw)', '+            if math.isnan(min_price) or math.isinf(min_price):', '+             raise ValueError', '        except ValueError:', '            return jsonify({"error": "minPrice must be a number"}), 400'], 'problem': "Indentation error: the 'raise ValueError' line is mis-indented (it has fewer spaces than the 'if' block). This will produce an IndentationError or otherwise break execution. The same mis-indentation appears for the max_price check later in the same function (see next block)."}
- {'file': 'techshop_testing/Server.py', 'hunk': '@@ -138,12 +138,16 @@', 'lines': ['        try:', '            max_price = float(max_price_raw)', '+            if math.isnan(max_price) or math.isinf(max_price):', '+             raise ValueError', '        except ValueError:', '            return jsonify({"error": "maxPrice must be a number"}), 400'], 'problem': 'Same indentation problem for the max_price NaN/Infinity check.'}
### Suggestions
- Fix the indentation for the newly added raise statements so they are properly nested inside the if-block. For example replace the two lines:
    if math.isnan(min_price) or math.isinf(min_price):
     raise ValueError
  with:
    if math.isnan(min_price) or math.isinf(min_price):
        raise ValueError
  (and apply the same fix for max_price).
- Add explicit handling for REQ-02 (category validation): in get_products() validate 'category' against the allowed set {'electronics', 'accessories'} and return (jsonify({"error": "Invalid category"}), 400) when invalid.
- Implement or verify REQ-01 search behavior: ensure the search is case-insensitive (compare lowercased product name and search term), return all products when search term is empty/whitespace-only, and return an empty array with status 200 when there are no matches.
- Implement REQ-03 range logic: after parsing minPrice and maxPrice, if both are provided and minPrice > maxPrice return (jsonify({"error": "minPrice cannot be greater than maxPrice"}), 400).
- Ensure REQ-04 combined filter logic applies all filters together (filter by name, category, and price range in a single pass) and add unit tests exercising combinations such as '?search=keyboard&category=electronics&maxPrice=150'.
- Verify REQ-05 single product endpoint behavior: GET /api/products/<id> should return the product JSON with 200 when found and (jsonify({"error": "Product not found"}), 404) when not found. Add tests for missing product case.
- Add or update automated tests to cover edge cases: NaN/Infinity min/max, minPrice > maxPrice, invalid category, whitespace-only search, case-insensitive matching, combined filters, and product-not-found.
- Consider adding defensive tests or input normalization (strip() the search term) and centralizing validation logic to improve maintainability.
### Quality notes
- The commit attempts to harden numeric parsing by rejecting NaN/Infinity which is a good idea, but the indentation bug makes the change unsafe and will break runtime.
- Only a small portion of the feature (numeric validation) was touched in this commit; there is no evidence in this diff that other requirements were added, fixed, or tested.
- No unit tests or changelog entries are included in the diff to demonstrate acceptance criteria are met.
- Keep commit messages focused on what was changed and why; consider breaking validation and feature additions into separate commits with tests.
