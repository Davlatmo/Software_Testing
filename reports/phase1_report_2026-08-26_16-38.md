# Phase 1 — Requirements Compliance Report
## Result: NON-COMPLIANT
### Summary
The commit only makes small changes to error messages and adds NaN/Inf checks for numeric parsing in techshop_testing/Server.py. It does not implement the core search, category filtering, combined filtering, or single-product endpoint behaviors required by the requirements.md. Several requirements are missing or only partially handled, and there is a likely indentation/formatting problem in the diff that could cause a runtime error.
### Missing requirements
- REQ-01: Search by name — case-insensitive name search implementation (GET /api/products?search=<term>), including treatment of empty/whitespace search returning all products
- REQ-02: Filter by category — validation of allowed categories (electronics, accessories) and returning 400 with { "error": "Invalid category" } for invalid categories
- REQ-03: Full price-range filtering implementation — while numeric validation was partially added, the code does not show applying minPrice/maxPrice to filter products (inclusive), and behavior when only one bound is provided is not demonstrated
- REQ-04: Combined filters — logic to combine search, category, and price filters together is not present
- REQ-05: Single product endpoint — GET /api/products/<id> returning product or 404 with { "error": "Product not found" } is not shown/verified
### Errors found
- techshop_testing/Server.py (hunk @@ -141,7 +141,7 @@): The + line replacing the max_price parsing error uses different indentation than surrounding lines ('+         return jsonify({"error": "maxPrice must be a number"}), 400'). The diff shows one fewer leading space compared to the original line and surrounding context which can produce an IndentationError or inconsistent indentation in Python.
- techshop_testing/Server.py (end of shown hunk): The diff ends with 'filtered = list(PRODUCTS)' but there is no subsequent code in this commit that applies any filters (search, category, minPrice/maxPrice) to 'filtered'. That means the request will return all products regardless of query params unless filtering logic exists elsewhere unchanged — the diff provides no evidence of implementing the filters required by REQ-01/REQ-02/REQ-03/REQ-04.
- No evidence in the diff that the required 'Invalid category' error response (exactly { "error": "Invalid category" }) is implemented when an invalid category is passed (REQ-02).
- No evidence in the diff of the single-product endpoint behavior (REQ-05): returning a single product by ID or 404 with { "error": "Product not found" } if missing.
- Inconsistent naming conventions between variables (snake_case like min_price) and JSON error keys (camelCase like 'minPrice'/'maxPrice') — not an absolute error, but may cause confusion and inconsistency across API responses.
### Suggestions
- Fix indentation: in techshop_testing/Server.py, ensure the line returning the maxPrice parse error is indented consistently with the surrounding try/except block. Match the exact indentation level used in the rest of the function to avoid IndentationError (the change is in the hunk starting at @@ -141,7 +141,7 @@).
- Implement search-by-name (REQ-01): inside get_products() apply a case-insensitive filter on product['name']. Example: normalize term = search_raw.strip().lower(); if term == '': treat as no search (return all); otherwise filter where term in product['name'].lower(). Add unit tests to cover empty/whitespace, no matches (returns [] with 200), and case-insensitivity.
- Implement category filtering and validation (REQ-02): accept only allowed categories = {'electronics','accessories'}. If category param provided but not in allowed list, return jsonify({"error": "Invalid category"}), 400 exactly as specified. Otherwise filter products by product['category'] == category.
- Complete price-range filtering (REQ-03): after parsing min_price and max_price (and keeping the NaN/Inf checks), filter products whose numeric price is between the bounds inclusive. If only min or max provided, apply single-bound filter. Keep the existing check and message for minPrice > maxPrice: return jsonify({"error": "minPrice cannot be greater than maxPrice"}), 400 (you already updated this message).
- Combine filters (REQ-04): apply the filters sequentially to 'filtered' (search, category, price) so that query parameters can be combined. Add tests for combined usage, e.g., ?search=keyboard&category=electronics&maxPrice=150.
- Implement single-product endpoint (REQ-05): ensure get_product(id) returns jsonify(product), 200 if found; otherwise return jsonify({"error": "Product not found"}), 404. Add tests for found and not-found cases.
- Ensure all endpoints return JSON and consistent error keys/messages per requirements. Consider standardizing error message casing across responses (requirements use camelCase for minPrice/minPrice comparison message and a specific string for invalid category).
- Add automated tests for all acceptance criteria: JSON responses, case-insensitive search, combination of filters, and correct 4xx codes for invalid inputs.
- Remove or avoid embedding non-essential inline markers (e.g., '← add this line') in committed code comments to keep codebase clean; while comments are valid they may include non-ASCII characters and clutter diffs.
### Quality notes
- Positive: The commit adds numeric validation for min_price and max_price including checks for NaN and infinity (math.isnan/math.isinf), which is a good robustness improvement.
- Positive: The error message for the minPrice>maxPrice case was updated to the camelCase wording expected by the requirements.
- Areas for improvement: The commit is very small and does not implement the core requested features. The lack of filter application after constructing 'filtered = list(PRODUCTS)' suggests incomplete work. Consider implementing complete unit/integration tests to ensure requirements are satisfied before committing.
- Stylistic: There is mixed naming between internal variables (snake_case) and JSON keys (camelCase). While JSON keys often differ from Python style, keep messages and keys consistent and documented in the API spec.
- Maintainability: Make sure validation rules (allowed categories, numeric parsing rules, exact error strings) are centralized or clearly documented so future changes remain consistent.
