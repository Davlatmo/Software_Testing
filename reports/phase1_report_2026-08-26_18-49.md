# Phase 1 — Requirements Compliance Report
## Result: NON-COMPLIANT
### Summary
The commit partially implements category and numeric price validation for the GET /api/products endpoint, but it does not implement search filtering, combined filter application (search + price), nor is there evidence of the single-product endpoint implementation. There are also a few implementation issues and omissions that prevent full compliance with the requirements document.
### Missing requirements
- REQ-01: Search by name — filtering by the 'search' query parameter (case-insensitive) is not implemented. Handling of empty/whitespace search term returning all products is not implemented.
- REQ-04: Combined filters — there is no evidence in the diff that search and price filters are applied together with category; only category filtering is shown.
- REQ-05: Single product endpoint — the commit references get_product() in comments but the diff does not show any implementation or changes to a GET /api/products/<id> handler.
### Errors found
- techshop_testing/Server.py (hunk starting at @@ -114,20 +114,20 @@): The variable 'search' is read: `search = request.args.get("search")` but there is no subsequent filtering logic in this diff to apply the search term to `filtered`. (Lines in diff around the get_products function show category and price parsing but no search filtering.)
- techshop_testing/Server.py (same hunk): The code applies only category filtering: `if category and category != "all": filtered = [p for p in filtered if p["category"] == category]`. There is no application of min_price/max_price to the `filtered` list in the shown diff, so price-based filtering is missing.
- techshop_testing/Server.py (top import hunk @@ -2,7 +2,7 @@): The import 'math' was removed. The old code had checks for math.isnan/isinf for numeric validation; removing the import also removed NaN/Inf validation but the error handling still accepts 'nan' and 'inf' strings as valid floats (float('nan') and float('inf') succeed). While the requirements don't explicitly mention NaN/Inf, this can lead to surprising behavior for numeric filters.
- techshop_testing/Server.py (hunk around min/max parsing @@ -135,29 +135,25 @@): The conversion of minPrice/maxPrice uses try/except ValueError which will not catch values like 'nan' or 'inf' (these convert to floats rather than raising ValueError). This may let non-finite numbers through, leading to odd filter behavior or comparisons. (This is a potential bug relative to robust numeric validation.)
- Behavioral mismatch: REQ-01 requires search to be case-insensitive and to return all products when the search term is empty/whitespace; neither behavior is implemented in the shown changes.
### Suggestions
- Implement search filtering inside get_products (file: techshop_testing/Server.py, in the get_products function near the category/price filtering code):
- After building `filtered = list(PRODUCTS)`, add logic like:
  if search is not None and search.strip() != "":
      term = search.strip().lower()
      filtered = [p for p in filtered if term in p.get('name','').lower()]
  # If search is None or whitespace-only, do not filter (return all products)
This satisfies REQ-01 (case-insensitive match, whitespace handling).
- Apply price filters to `filtered` as well (in the same get_products function):
- If min_price is not None: filtered = [p for p in filtered if float(p.get('price', 0)) >= min_price]
- If max_price is not None: filtered = [p for p in filtered if float(p.get('price', 0)) <= max_price]
This ensures REQ-03 and REQ-04 (combined filters) are enforced.
- Reintroduce robust numeric checks or explicitly handle 'nan'/'inf' strings if you want to reject them (optional but recommended):
- Either re-add import math and after float() check math.isfinite(value) (i.e., not math.isnan and not math.isinf), or explicitly check for these string values before conversion and return a 400 with appropriate message.
Example:
  try:
      v = float(min_price_raw)
      if not math.isfinite(v):
          raise ValueError
  except ValueError:
      return jsonify({"error": "minPrice must be a number"}), 400
- Implement the single-product endpoint GET /api/products/<id> (REQ-05) if missing. Example implementation in techshop_testing/Server.py:
- def get_product(product_id):
    p = next((x for x in PRODUCTS if x['id'] == product_id), None)
    if not p:
        return jsonify({"error": "Product not found"}), 404
    return jsonify(p), 200
Ensure the route decorator matches the API path used by the app.
- Add unit/integration tests exercising combinations of filters: search+category, search+price, category+price, and all three combined (REQ-04 + acceptance criteria).
### Quality notes
- The commit contains small unrelated whitespace changes (e.g., spacing after `.lower()` assignments). Consider keeping whitespace changes to a minimum to improve diff readability.
- There is a helpful use of constants (VALID_CATEGORIES) and early validation for category, which is good. Allowing an explicit 'all' category value is acceptable if documented, but it's not mentioned in the requirements; consider documenting it or removing special-casing if not desired.
- Error messages returned match the phrasing in the requirements for invalid category and min/max ordering, which is good.
- The code currently reads all query parameters early, which is clear. To improve maintainability, factor filtering steps into small helper functions (e.g., _apply_search_filter, _apply_category_filter, _apply_price_filter) so combined filtering is simpler to reason about and test.
- Missing unit tests and lack of demonstration of get_product implementation reduce confidence in full compliance.
