# Phase 1 — Requirements Compliance Report
## Result: NON-COMPLIANT
### Summary
The commit partially updates the products endpoint (category validation and numeric parsing), but it does not fully implement or clearly satisfy several requirements from the spec. Search behaviour (REQ-01) and combined-filters behaviour (REQ-04) are not visible/verified in the diff, empty/whitespace query parameter handling for search and prices is not robust, and the handler accepts a special-case category value "all" which is not specified in the requirements. There are also minor code-quality issues and leftover comments suggesting incomplete changes.
### Missing requirements
- REQ-01: Explicit handling of empty or whitespace-only search terms (should return all products). The diff does not show any trimming/whitespace handling for search parameter.
- REQ-01: Explicit confirmation/implementation of case-insensitive name search. The diff does not show the search filter implementation being adjusted/verified to be case-insensitive.
- REQ-04: Clear proof that search, category and price filters are combined correctly (the diff shows category filtering but not combined-filter logic or tests demonstrating combination).
- Acceptance: Robust handling of empty/whitespace query parameters for minPrice/maxPrice (treat empty/whitespace as absent). The diff uses `is not None` which will treat empty strings as present and produce 400 rather than ignoring them.
### Errors found
- techshop_testing/Server.py (hunk starting @@ -2,7 +2,7 @@): The import of math was removed. The earlier code had comments indicating intent to check for NaN/Inf (math.isnan/math.isinf); if such checks are required they are now missing. If you do not intend to check NaN/Inf then remove the related comments; otherwise re-add `import math` and corresponding checks.
- techshop_testing/Server.py (hunk starting @@ -114,20 +114,20 @@): Category handling accepts the special value "all": the code checks `if category != "all" and category not in VALID_CATEGORIES:` and allows "all" to skip the 400. The requirements list only two valid categories (`electronics`, `accessories`) and do not mention an "all" value. Accepting "all" is a deviation from the specified valid-category semantics.
- techshop_testing/Server.py (hunk starting @@ -135,29 +135,25 @@): Price parsing uses `if min_price_raw is not None:` and `if max_price_raw is not None:`. request.args.get(...) returns an empty string when the parameter is present but blank; using `is not None` will attempt to parse blank/whitespace strings and return 400 (`must be a number`). The requirements do not specify behavior for blank price params, but typical expected behaviour is to treat empty/whitespace as "not provided" and not error. The current handling can produce surprising 400 responses for e.g. `?minPrice=`.
- techshop_testing/Server.py (multiple hunks): There are leftover inline notes in the diff (e.g. comments with arrows `# ← add this line`) and inconsistent whitespace edits. These suggest incomplete edits and reduce readability/maintainability.
- Unverified: GET /api/products/<id> behavior (REQ-05) is not visible in this diff. The docstring mentions `get_product()` handles single-product; but the commit did not modify or show that implementation, so correct 404/json behavior cannot be confirmed from this diff.
### Suggestions
- Search handling (REQ-01): In get_products(), normalize and trim the search param before applying the filter. Example fix: `if search is not None: term = search.strip(); if term == "": term = None; else: term = term.lower()` then filter with `p['name'].lower()` (e.g. `filtered = [p for p in filtered if term in p['name'].lower()]`). This ensures case-insensitive matching and treats blank/whitespace search as "no filter" (return all products).
- Price parsing robustness: change the checks to ignore empty/whitespace values. For example use `if min_price_raw is not None and min_price_raw.strip() != "":` before parsing, and similarly for max_price_raw. This prevents accidental 400s for `?minPrice=`. After parsing, keep the existing numeric validation and the min>max check.
- NaN/Inf checks (optional): If you want to reject NaN/Inf explicitly, re-add `import math` at the top and after converting to float check `if math.isnan(min_price) or math.isinf(min_price): raise ValueError`. Remove the `# ← add this line` comment markers when actually implementing the checks.
- Category handling: Remove implicit acceptance of "all" unless that is an intended feature. To strictly implement the requirements change `if category:` block to `if category:` -> `category = category.strip().lower()` then `if category not in VALID_CATEGORIES:` return 400. If you intend to keep `all`, update the requirements doc to include it and add a comment explaining the reason.
- Combined-filter verification: Ensure filters are applied together by chaining them on the same `filtered` list. Add unit tests for combinations such as `?search=keyboard&category=electronics&maxPrice=150` to make sure results match REQ-04. Verify ordering: apply category->search->price (or any order) but ensure all filters are intersected.
- Single product endpoint (REQ-05): If not already implemented, add/verify `GET /api/products/<id>` returns JSON with 200 for found product and `{ "error": "Product not found" }` with 404 when missing. Add tests for both cases.
- Clean up comments and whitespace: remove debugging arrows/comments left in diff and standardize formatting to improve readability and maintainability.
### Quality notes
- The commit focuses on defensive parsing (try/except) for numeric params, which is good, but the current checks are brittle with respect to empty/whitespace inputs.
- There are several cosmetic whitespace changes and leftover inline notes (e.g. `# ← add this line`) that indicate the change was not fully cleaned up before commit.
- Error messages appear consistent with the requirements for the min>max case and invalid numeric inputs (once the whitespace handling is fixed).
- The code currently lowercases the incoming category parameter (good for case-insensitivity), but it compares directly to product["category"]; ensure product data uses normalized category values (lowercase) or compare normalized values on both sides.
- Add unit/integration tests covering: case-insensitive search, blank search term, invalid/blank numeric params, NaN/Inf if you intend to reject them, invalid category, and combined filters to prevent regressions.
