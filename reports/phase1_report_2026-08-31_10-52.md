# Phase 1 — Requirements Compliance Report
## Result: NON-COMPLIANT
### Summary
The committed changes in techshop_testing/Server.py do not fully implement the product search and filtering requirements. Key pieces are missing or altered in ways that break or weaken required behavior: the search-by-name behavior and the price filtering application are not present in the diff, validation for NaN/Infinity values was removed, and the code now accepts a special category value 'all' that is not described in the requirements. Some required error handling appears but other required functionality is not shown in the diff.
### Missing requirements
- REQ-01: Case-insensitive search-by-name logic that returns only products whose name matches the search term, treats empty or whitespace-only search as 'return all', and returns an empty array (200) when there are no matches. (No search filtering implementation is present in the diff for get_products().)
- REQ-03: Application of minPrice/maxPrice filtering to the products list (inclusive bounds). The diff shows parsing of minPrice/maxPrice but does not show applying the parsed min_price/max_price to the filtered product list.
- REQ-04: Evidence that all filters (search, category, price) are combined correctly (the diff only shows category filtering; search & price application are missing).
- Acceptance: Proof that search is case-insensitive (no search implementation shown to confirm this).
- Acceptance: Proof that empty/whitespace search returns all products (not implemented in diff).
### Errors found
- Server.py: top hunk @@ -2,7 +2,7 @@ - the import 'import math' was removed (line shown as '-import math'). Later code previously relied on math.isnan/math.isinf checks; the removal breaks NaN/Infinity validation that prevented invalid numeric inputs. (See the old commented lines around the minPrice/maxPrice parsing shown in the diff.)
- Server.py: get_products() hunk @@ -135,29 +135,25 @@ - the checks for math.isnan and math.isinf were removed from both minPrice and maxPrice parsing. The diff shows the previously present lines '-          if math.isnan(min_price) or math.isinf(min_price):   # ← add this line' and similar for max_price were removed. This allows values like 'NaN' or 'Infinity' to be accepted as numeric (float('nan')/float('inf')) which will produce unexpected behavior when filtering and when comparing min_price and max_price (NaN comparisons always fail).
- Server.py: get_products() hunk @@ -114,20 +114,20 @@ - There is no implementation in the diff that filters 'filtered' by search term (search variable is read but never used in the shown diff). Without this, REQ-01 is not implemented. The diff shows 'search = request.args.get("search")' but no subsequent filtering by product name.
- Server.py: get_products() hunk @@ -135,29 +135,25 @@ - After parsing min_price and max_price the diff does not show applying numeric range filters to the 'filtered' list (no list comprehension or filtering by p['price'] is present in the shown changes). Thus REQ-03 behavior (return products whose price is within the range inclusive) is not implemented in this diff.
- Server.py: get_products() hunk @@ -114,20 +114,20 @@ - The code treats the category value 'all' as acceptable and bypasses the VALID_CATEGORIES check: 'if category != "all" and category not in VALID_CATEGORIES:'. The requirements only list valid categories as 'electronics' and 'accessories' and do not define 'all' as valid; accepting 'all' may let a user pass a value that isn't one of the documented valid categories without triggering the required 400 Invalid category response.
### Suggestions
- Re-add robust numeric validation for minPrice and maxPrice: re-import math (add 'import math' near the top) and use math.isfinite() to reject NaN/Infinity values, or check via 'if not math.isfinite(value): raise ValueError'. Example fix in Server.py (around the minPrice/maxPrice parsing, hunk @@ -135,29 +135,25 @@):
- import math
- ...
- try:
-     min_price = float(min_price_raw)
-     if math.isnan(min_price) or math.isinf(min_price):
-         raise ValueError
- except ValueError:
-     return jsonify({"error": "minPrice must be a number"}), 400
+ import math
+ ...
+ try:
+     min_price = float(min_price_raw)
+     if not math.isfinite(min_price):
+         raise ValueError
+ except ValueError:
+     return jsonify({"error": "minPrice must be a number"}), 400
Do the same for maxPrice parsing. This prevents NaN/Inf sneaking through and causing silent logic errors.
- Implement search filtering according to REQ-01 in get_products(): after initializing 'filtered = list(PRODUCTS)', add a case-insensitive name filter and treat empty/whitespace-only search as returning all products. Example logic (in Server.py inside get_products(), after category filter):
- if search is not None:
-     search_term = search.strip()
-     if search_term:
-         lower = search_term.lower()
-         filtered = [p for p in filtered if lower in p.get('name','').lower()]
This ensures search is case-insensitive and that empty/whitespace search returns unfiltered list.
- Apply min_price/max_price filtering to 'filtered' (REQ-03 and REQ-04): after parsing numeric values and after category/search filtering, add:
- if min_price is not None:
-     filtered = [p for p in filtered if p.get('price') >= min_price]
- if max_price is not None:
-     filtered = [p for p in filtered if p.get('price') <= max_price]
Ensure p['price'] is the numeric field in product dicts. This ensures inclusive bounds and that filters combine correctly.
- Remove or explicitly document the special-case 'all' category: if you want to support 'all' treat it as equivalent to omitting the category parameter, but the requirements do not list 'all' as a valid category. To strictly follow requirements change the validation to:
- if category and category not in VALID_CATEGORIES:
-     return jsonify({"error": "Invalid category"}), 400
or if you keep 'all', document it in requirements and in API docs.
- Confirm single-product endpoint behavior (REQ-05): verify that there is a GET /api/products/<id> route implementation (function name likely get_product()) that returns the product JSON and returns 404 with {"error":"Product not found"} when absent. If not present, implement:
- product = next((p for p in PRODUCTS if p['id'] == id), None)
- if not product:
-     return jsonify({"error": "Product not found"}), 404
- return jsonify(product), 200
- Add unit tests or integration tests for these behaviors: search case-insensitivity and whitespace handling, numeric parsing including NaN/Infinity, inclusive min/max behavior, combined filters, invalid category handling, and single-product 404 behavior. Tests will prevent regressions such as the removal of numeric checks.
### Quality notes
- Whitespace and indentation changes are present in the diff (multiple lines show spacing differences). Keep formatting consistent to avoid noisy diffs.
- There are leftover inline comments in the diff (e.g. comments referencing '← add this line') which suggest unfinished changes; clean up or update those comments to reflect the current code.
- Error responses consistently use JSON with an 'error' key (good). Ensure messages match exactly the requirement strings where specified.
- The diff modifies validation behavior (removing NaN/Inf checks) which reduces robustness. Prefer explicit checks (math.isfinite) rather than relying on float conversion only.
- The code currently reads request args correctly and starts filtering in a reasonable order (category first); however, without applying all filters to the same 'filtered' list the combined-filter requirement is not satisfied.
- No changes to tests or addition of new tests were provided in this commit; adding tests for each REQ would improve confidence.
