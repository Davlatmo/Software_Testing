# Phase 1 — Requirements Compliance Report
## Result: NON-COMPLIANT
### Summary
The committed changes partially implement the product-list filtering endpoints, but they introduce at least one clear deviation from the requirements (allowing the special category value "all") and do not demonstrate that search behavior and some other acceptance criteria are satisfied. Several requirements are missing or cannot be verified from the diff.
### Missing requirements
- REQ-01: Explicit implementation of case-insensitive name search that treats empty or whitespace-only search terms as 'return all products'. (No search-filtering logic is visible in the changed hunk.)
- REQ-02: Strict category validation per requirements: only 'electronics' and 'accessories' should be accepted. The code treats 'all' as valid, which is not listed in requirements.
- REQ-03: Robust handling of NaN/Infinity for numeric price inputs (the math.isnan/math.isinf checks were removed). If this behavior was desired it is now missing.
- REQ-04: Evidence that all filters (search, category, minPrice/maxPrice) can be combined correctly (the diff only shows category filter; price and search filtering are not present in the changed lines).
- REQ-05: Verification that GET /api/products/<id> returns JSON and a 404 with { "error": "Product not found" } when missing. The diff references get_product() but does not show or change it, so its behavior cannot be confirmed from this commit.
### Errors found
- techshop_testing/Server.py @@ -114,20 +114,20: the new code allows the query param 'category' == "all" to bypass the 'Invalid category' 400 check: +        if category != "all" and category not in VALID_CATEGORIES:. Requirements define valid categories only as 'electronics' and 'accessories' and require 400 for invalid values. Allowing 'all' violates REQ-02.
- techshop_testing/Server.py @@ -135,29 +135,25: removal of import math at top (diff shows 'import math' removed) and removal of math.isnan/math.isinf checks from minPrice/maxPrice parsing. If the system must reject NaN/Infinity explicitly this check has been removed; otherwise the code will accept floats that are NaN/inf if float() were to produce them (rare from user input, but the explicit checks were removed).
- techshop_testing/Server.py @@ -114,20 & @@ -135,29: No visible implementation in these hunks for case-insensitive substring matching on product name (REQ-01) or trimming/handling whitespace-only search terms to return all products. The 'search' variable is retrieved (search = request.args.get("search")) but no filtering by name is shown in the changed region.
- techshop_testing/Server.py @@ -135,29: the code checks min_price_raw is not None before parsing. If a client supplies an empty string for minPrice (e.g. ?minPrice=) request.args.get returns '' which is not None; float('') raises ValueError and code returns 400 'minPrice must be a number' which may be acceptable, but requirements do not specify behavior for empty param values — add explicit handling if empty should be treated as omitted.
### Suggestions
- Fix category validation to match REQ-02: do not special-case 'all'. Replace the check at techshop_testing/Server.py @@ -114 (around the line '+        if category != "all" and category not in VALID_CATEGORIES:') with a strict membership check, for example:
-    if category:
-        category = category.lower()
-        if category not in VALID_CATEGORIES:
-            return jsonify({"error": "Invalid category"}), 400
This enforces only 'electronics' and 'accessories' as valid values.
- Implement or verify case-insensitive search and whitespace handling for REQ-01. Concrete code to add (in get_products, after initializing filtered):
-    if search is not None:
-        term = search.strip()
-        if term != "":
-            term = term.lower()
-            filtered = [p for p in filtered if term in p.get('name','').lower()]
-        # if term is empty/whitespace, do not filter (return all products)
This ensures substring matching is case-insensitive and whitespace-only search returns all products.
- Ensure price filters are applied to 'filtered' and that range is inclusive (REQ-03). Example snippets to add after parsing min_price/max_price:
-    if min_price is not None:
-        filtered = [p for p in filtered if float(p.get('price', 0)) >= min_price]
-    if max_price is not None:
-        filtered = [p for p in filtered if float(p.get('price', 0)) <= max_price]
Also keep the existing min_price > max_price 400 check.
- If you want to reinstate NaN/Infinity validation, re-add 'import math' at the top and check math.isnan/math.isinf on the parsed floats (or explicitly reject parse results that are not finite). If you prefer to omit those checks, document the decision in a comment.
- Confirm GET /api/products/<id> behavior in the repository (get_product function): ensure it returns application/json and on missing product returns (jsonify({"error": "Product not found"}), 404). If it's not implemented, implement it accordingly.
- Harden query param parsing: treat empty-string parameters as omitted when appropriate. For example use 'raw = request.args.get("minPrice")
if raw is not None and raw.strip() != "": parse it; else treat as None' so '?minPrice=' does not produce an unexpected 400 unless that is desired.
### Quality notes
- The diff shows small formatting/whitespace adjustments but no major structural changes. Removing the unused 'math' import cleans up dependencies, but also removed NaN/inf checks — ensure that was intentional.
- VALID_CATEGORIES is defined inside get_products; it's acceptable but could be moved to a module-level constant for reuse and clarity.
- Error responses are JSON and include clear messages, which is good. Make sure all endpoints consistently return JSON for both success and error cases (REQ acceptance criteria).
- The commit message and inline comments reference implemented REQs, but the actual code should be audited to ensure each is enforced. Relying on comments without test coverage can be misleading.
