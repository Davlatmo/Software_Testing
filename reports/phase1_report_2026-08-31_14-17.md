# Phase 1 — Requirements Compliance Report
## Result: NON-COMPLIANT
### Summary
Implementation mostly satisfies the product search and filtering requirements, but the committed change altered the exact error response for an invalid category which violates REQ-02 (the response body must be exactly { "error": "Invalid category" }).
### Missing requirements
- REQ-02: Return body must be exactly { "error": "Invalid category" } when an invalid category is provided.
### Errors found
- techshop_testing/Server.py — get_products(): In the diff hunk @@ -129,9 +129,9 @@ def get_products() the returned error JSON for an invalid category was changed to include an extra exclamation mark. The diff shows the line changed from return jsonify({"error": "Invalid category"}), 400 to return jsonify({"error": "Invalid category!"}), 400. This no longer matches the requirement's exact response body for REQ-02.
### Suggestions
- In techshop_testing/Server.py, inside get_products(), revert the invalid-category error message to match the requirement exactly. Change return jsonify({"error": "Invalid category!"}), 400 back to return jsonify({"error": "Invalid category"}), 400 (line shown in diff hunk @@ -129,9 +129,9 @@).
- Add or update unit tests that assert the exact JSON body for invalid category inputs (status 400 and body { "error": "Invalid category" }) to prevent accidental message changes in future commits.
### Quality notes
- Search implementation is correct: whitespace-only search returns all products, and substring, case-insensitive search is used (get_products()).
- Price parsing is robust: minPrice/maxPrice are explicitly parsed to float and NaN/Infinity are rejected with 400 responses.
- Combined filtering (category -> search -> price) is applied in a logical order and will produce correct intersected results.
- Single-product endpoint (GET /api/products/<id>) correctly returns 404 with { "error": "Product not found" } when appropriate.
- Minor observation: the code registers two @app.after_request functions (set_session_cookie and add_cors_headers). Flask supports multiple after_request handlers, but be mindful of ordering if future changes depend on a specific sequence.
- Consider adding tests that assert exact response bodies (not just status codes) where the requirements specify exact JSON content.
